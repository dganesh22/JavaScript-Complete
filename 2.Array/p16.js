/* 
    array.slice(start,ending)
*/

var colors = ["green", "red", "blue", "orange", "slateblue","white"];
console.log(`colors =`, colors)

let arr = colors.slice(1,4)

console.log(`after slice =`, arr)