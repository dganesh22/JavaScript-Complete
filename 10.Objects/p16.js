// Object.keys()
// Object.values()

let book = {
    title: "Javascript",
    pages: 200,
    isbn: "13245afa" 
}

console.log(`book = `, book)

// array of keys
console.log(`keys = `, Object.keys(book))

//array of values
console.log(`values =`, Object.values(book))